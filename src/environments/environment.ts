export const environment = {
  production: true,
  // In production the frontend is served behind the same origin as the gateway.
  // Set to empty string so all /api/* requests are relative (same-origin).
  apiUrl: ''
};
